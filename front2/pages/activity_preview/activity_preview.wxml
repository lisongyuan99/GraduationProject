<navbar parameter='{{parameter}}'></navbar>
<view class='product-con'>
  <view id="past0">
    <productConSwiper imgUrls="{{activityInfo.pics}}"></productConSwiper>
    <view class='wrapper'>
      <view class='share acea-row row-between row-bottom'>
        <view class='money font-color' wx:if="{{activityInfo.free}}">
          <text class='num'>免费</text>
        </view>
        <view class='money font-color' wx:else>
          <text class='num'>￥{{activityInfo.price}}</text>
          <text class='ori'>￥{{activityInfo.ori}}</text>
        </view>
        <view class="vote_panel">
          <view class="vote_button">
            <image src="/images/up.svg" class="vote"></image>赞{{activityInfo.up}}
          </view>
          <view class="vote_button">
            <image src="/images/down.svg" class="vote"></image>踩{{activityInfo.down}}
          </view>
        </view>
      </view>
      <view class='introduce'>{{activityInfo.name}}</view>
      <view class='label acea-row row-between-wrapper'>
        <view>{{activityInfo.description}}</view>
      </view>
      <view class='label acea-row row-between-wrapper'>
        <view>剩余{{activityInfo.rest}}件</view>
        <view>参与人数{{activityInfo.joinUserNum}}</view>
        <view>浏览{{activityInfo.viewUserNum}}</view>
      </view>
      <view class='label acea-row row-between-wrapper'>
        <view>活动时间{{activityInfo.time}}</view>
      </view>

    </view>
    <view class="fan acea-row row-between-wrapper">
      <view class="picTxt acea-row row-between-wrapper">
        <view class="pictrue">
          <image wx:if="{{activityInfo.orgAvatar}}" src="{{activityInfo.orgAvatar}}"></image>
          <image wx:else src="{{activityInfo.orgAvatar}}"></image>
        </view>

        <view class="text">
          <view class="acea-row row-middle">
            <view wx:if="{{activityInfo.orgName}}" class="name line1">发布商家:{{activityInfo.orgName}}</view>
            <view wx:else class="name line1">发布商家:无名氏</view>
          </view>
          <view>
            <text wx:if="{{activityInfo.motto}}">{{activityInfo.motto}}</text>
            <text wx:else>这个人很懒，什么也没留下</text>
          </view>
        </view>
      </view>
    </view>
  </view>

  <view class='product-intro' id="past3" wx:if="{{activityInfo.regionCode!==0}}">
    <view class='title'>活动地址:{{activityInfo.address}}</view>
    <map id="myMap" class="my-map" latitude="{{activityInfo.lat}}" longitude="{{activityInfo.lng}}"
      markers="{{markers}}" show-location />
  </view>
  <view class='product-intro' id="past3">
    <view class='title'>产品介绍</view>
    <view class='conter'>
      <rich-text nodes="{{activityInfo.detail}}"></rich-text>
    </view>
  </view>
  <view style='height:120rpx;'></view>

</view>

<view class="buttom-paddle">
  <view class="customer-panel" bindtap="toOrderPage">
    <van-icon name="friends-o" />
    <view>相关订单:{{activityInfo.followUserNum}}</view>
    <van-icon name="arrow" />
  </view>
  <view class="customer-panel" bindtap="openSuggestion">
    <van-icon wx:if="{{ activityInfo.suggestionAmount!=0}}" name="fire-o" color="#e93323"/>
    <van-icon wx:else name="fire-o"></van-icon>
    <view>
      <text wx:if="{{ activityInfo.suggestionAmount!=0}}"> 已推荐{{activityInfo.suggestionAmount}}</text>
      <text wx:else>未推荐</text>
    </view>
    <van-icon name="arrow" />
  </view>
  <view class="button-panel">
    <view class="icon-button" bindtap="editActivity">
      <van-icon name="edit" size="50rpx" color="#e93323" />
      <text>编辑</text>
    </view>
    <view class="icon-button" bindtap="deleteActivity">
      <van-icon name="close" size="50rpx" color="#e93323" />
      <text>删除</text>
    </view>
  </view>
</view>

<van-dialog use-slot title="推荐金额" show="{{ showSuggestion }}" show-cancel-button  bind:confirm="addSuggest">
   <view class="input-outer">
      <input class="input-inner" type="digit" model:value="{{addSuggestPrice}}" bindinput="empty"></input>
   </view>
</van-dialog>